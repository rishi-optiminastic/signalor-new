/**
 * Central route table. Keep all app paths here so links/redirects stay in sync.
 */
export const routes = {
  home: '/',
  signIn: '/sign-in',
  signUp: '/sign-up',
  onboarding: '/onboarding',
  pricing: '/pricing',
  loading: '/loading',
  dashboard: '/dashboard',
} as const
