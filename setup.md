# Setup

This file used to be a bootstrap guide. The project is now fully wired — see [`README.md`](./README.md) for:

- Feature list and dependency reference
- Project structure and path aliases
- Quick start (install → env → db → dev)
- Available scripts (`dev`, `lint`, `type-check`, `db:*`, ...)
- Environment variable reference
- Git hooks, CI, and authoring rules

For coding conventions and architectural rules, see [`CLAUDE.md`](./CLAUDE.md).
